export type Image = { url: string; altText?: string | null; width?: number; height?: number };
export type Product = { id: string; handle: string; title: string; description: string; featuredImage?: Image | null; images: Image[]; priceRange: { minVariantPrice: { amount: string; currencyCode: string } }; variants: { id: string; title: string; availableForSale: boolean; price: { amount: string; currencyCode: string }; selectedOptions: { name: string; value: string }[] }[]; tags?: string[] };
export type Cart = { id: string; checkoutUrl: string; totalQuantity: number; cost: { totalAmount: { amount:string; currencyCode:string } }; lines: { id:string; quantity:number; merchandise: { id:string; title:string; product:{title:string; handle:string; featuredImage?:Image|null}; price:{amount:string;currencyCode:string} } }[] };
