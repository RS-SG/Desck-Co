import { Navigation } from '@/components/navigation'; import { CartDrawer } from '@/components/cart-drawer'; import { getCollection,getProducts } from '@/lib/shopify'; import { mockProducts } from '@/lib/mock-products'; import { ProductCard } from '@/components/product-card';
export default async function Collection({params}:{params:Promise<{handle:string}>}){const{handle}=await params;const collection=await getCollection(handle);const products=collection?.products.length?collection.products:(await getProducts(24)).length?await getProducts(24):mockProducts;return <main><Navigation/><CartDrawer/><section className="container pt-36 pb-16"><p className="eyebrow">Desck & Co / Collection</p><h1 className="mt-5 text-5xl tracking-display md:text-7xl">{collection?.title||'The complete edit.'}</h1><p className="mt-5 max-w-md text-sm leading-7 text-zinc-400">Purposeful pieces for the spaces that hold your days.</p></section><section className="container pb-24"><div className="grid gap-x-5 gap-y-12 sm:grid-cols-2 lg:grid-cols-3">{products.map((p,i)=><ProductCard key={p.id} product={p} index={i}/>)}</div></section></main>}
